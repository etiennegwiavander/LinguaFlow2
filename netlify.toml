[build]
  publish = ".next"
  command = "npm run build:netlify"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"
  NODE_OPTIONS = "--max-old-space-size=4096"
  NEXT_PUBLIC_APP_URL = "https://linguaflow.online"

# Increase function timeout for AI generation (max 26 seconds on free tier)
[functions]
  node_bundler = "esbuild"
  
[[functions]]
  path = "api/supabase/functions/generate-vocabulary-words"
  node_bundler = "esbuild"

# Next.js specific configuration
[[plugins]]
  package = "@netlify/plugin-nextjs"

# Let Next.js handle all routing including API routes
# No manual redirects needed - the plugin handles everything

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

[[headers]]
  for = "/_next/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"